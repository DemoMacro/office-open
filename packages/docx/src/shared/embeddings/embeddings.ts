/**
 * Embeddings module for WordprocessingML documents.
 *
 * The collection itself lives in core (shared with the other format
 * packages); this module keeps the docx import path stable.
 *
 * @module
 */

export { EmbeddingCollection, type EmbeddingData } from "@office-open/core";

/**
 * Extended (App) Properties — re-exported from @office-open/core.
 *
 * The descriptor and types live in core (shared OPC part, identical across
 * docx/pptx/xlsx). This module preserves the docx import path.
 *
 * @module
 */

export {
  appPropertiesDesc,
  type AppPropertiesOptions,
  type AppPropertiesInput,
} from "@office-open/core";

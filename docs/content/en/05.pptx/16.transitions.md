---
title: "Transitions"
description: "Add slide transition effects such as fade, push, wipe, and more"
---

Use the `transition` property on slide options to add animated effects when moving between slides.

## Basic Transition

::api-example{type="pptx"}

```json [JSON]
{
  "slides": [
    {
      "children": [],
      "transition": { "type": "fade", "speed": "med" }
    }
  ]
}
```

```ts [Class]
{
  children: [],
  transition: { type: "fade", speed: "med" },
}
```

::

## Slide Show Settings

Control presentation playback behavior with the `show` option on `Presentation`:

::api-example{type="pptx"}

```json [JSON]
{
  "show": {
    "loop": true,
    "useTimings": true
  },
  "slides": [
    {
      "children": [{ "shape": { "x": 0, "y": 0, "width": 200, "height": 100, "text": "Slide 1" } }]
    }
  ]
}
```

```ts [Class]
const pres = new Presentation({
  show: {
    loop: true,
    useTimings: true,
  },
  slides: [{ children: [new Shape({ x: 0, y: 0, width: 200, height: 100, text: "Slide 1" })] }],
});
```

::

### Show Options

| Property        | Type      | Description                       |
| --------------- | --------- | --------------------------------- |
| `loop`          | `boolean` | Loop presentation continuously    |
| `kiosk`         | `boolean` | Run in kiosk (full-screen) mode   |
| `showNarration` | `boolean` | Play narration during slide show  |
| `useTimings`    | `boolean` | Use recorded timings if available |

## Transition Types

### Fade

```ts
transition: {
  type: "fade";
}
```

### Push

```ts
transition: { type: "push", direction: "right" }
// direction: "left" | "right" | "up" | "down"
```

### Wipe

```ts
transition: { type: "wipe", direction: "down" }
```

### Cover

```ts
transition: { type: "cover", direction: "right" }
```

### Split

```ts
transition: { type: "split", orient: "horz", direction: "out" }
// orient: "horz" | "vert"
// direction: "in" | "out"
```

### Wheel

```ts
transition: { type: "wheel", spokes: 4 }
```

### Dissolve

```ts
transition: { type: "dissolve", speed: "slow" }
```

### Random

```ts
transition: {
  type: "random";
}
```

## Transition Options

| Property           | Type                        | Description                        |
| ------------------ | --------------------------- | ---------------------------------- |
| `type`             | `TransitionType`            | Transition type                    |
| `speed`            | `"slow" \| "med" \| "fast"` | Transition speed                   |
| `direction`        | `string`                    | Direction (depends on type)        |
| `orient`           | `"horz" \| "vert"`          | Orientation (split/blinds/checker) |
| `spokes`           | `number`                    | Number of spokes (wheel)           |
| `advanceOnClick`   | `boolean`                   | Advance on mouse click             |
| `advanceAfterTime` | `number`                    | Auto-advance after milliseconds    |

### Available Types

`"fade"` | `"push"` | `"wipe"` | `"split"` | `"blinds"` | `"checker"` | `"comb"` | `"randomBar"` | `"cover"` | `"pull"` | `"strips"` | `"wheel"` | `"zoom"` | `"circle"` | `"dissolve"` | `"diamond"` | `"newsflash"` | `"plus"` | `"wedge"` | `"random"` | `"cut"`

## Full Example

::api-example{type="pptx"}

```json [JSON]
{
  "slides": [
    {
      "children": [
        {
          "shape": {
            "x": 100,
            "y": 150,
            "width": 500,
            "height": 100,
            "text": "Welcome",
            "fill": "4472C4"
          }
        }
      ],
      "transition": { "type": "fade", "speed": "med" }
    },
    {
      "children": [
        {
          "shape": {
            "x": 100,
            "y": 150,
            "width": 500,
            "height": 100,
            "text": "Content Slide",
            "fill": "ED7D31"
          }
        }
      ],
      "transition": { "type": "push", "direction": "right", "speed": "slow" }
    },
    {
      "children": [
        {
          "shape": {
            "x": 100,
            "y": 150,
            "width": 500,
            "height": 100,
            "text": "Thank You",
            "fill": "70AD47"
          }
        }
      ],
      "transition": { "type": "wipe", "direction": "down" }
    }
  ]
}
```

```ts [Class]
{
  slides: [
    {
      children: [
        new Shape({
          x: 100,
          y: 150,
          width: 500,
          height: 100,
          text: "Welcome",
          fill: "4472C4",
        }),
      ],
      transition: { type: "fade", speed: "med" },
    },
    {
      children: [
        new Shape({
          x: 100,
          y: 150,
          width: 500,
          height: 100,
          text: "Content Slide",
          fill: "ED7D31",
        }),
      ],
      transition: { type: "push", direction: "right", speed: "slow" },
    },
    {
      children: [
        new Shape({
          x: 100,
          y: 150,
          width: 500,
          height: 100,
          text: "Thank You",
          fill: "70AD47",
        }),
      ],
      transition: { type: "wipe", direction: "down" },
    },
  ],
}
```

::

export interface ControlOptions {
  /** Shape id (AG_Ole `@spid`, ST_ShapeID token, e.g. "_x0000_s1026"). */
  shapeId?: string;
  name?: string;
  showAsIcon?: boolean;
  rId?: string;
  imageWidth?: number;
  imageHeight?: number;
}

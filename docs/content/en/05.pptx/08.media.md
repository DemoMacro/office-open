---
title: "Media"
description: "Embed video and audio files into slides with poster images"
---

Embed video and audio content into your presentations using `VideoFrame` and `AudioFrame`.

## Video

```ts
import { VideoFrame } from "@office-open/pptx";
```

::api-example{type="pptx"}

```json [JSON]
{
  "slides": [
    {
      "children": [
        {
          "video": {
            "x": 1,
            "y": 1,
            "width": 6,
            "height": 4,
            "data": "<Uint8Array>",
            "type": "mp4"
          }
        }
      ]
    }
  ]
}
```

```ts [Class]
{
  children: [
    new VideoFrame({
      x: 1,
      y: 1,
      width: 6,
      height: 4,
      data: new Uint8Array(fs.readFileSync("video.mp4")),
      type: "mp4",
    }),
  ],
}
```

::

## Video with Poster Image

A poster image is shown before the video starts playing:

::api-example{type="pptx"}

```json [JSON]
{
  "slides": [
    {
      "children": [
        {
          "video": {
            "x": 1,
            "y": 1,
            "width": 6,
            "height": 4,
            "data": "<Uint8Array>",
            "type": "mp4",
            "poster": "<Uint8Array>",
            "posterType": "jpg"
          }
        }
      ]
    }
  ]
}
```

```ts [Class]
new VideoFrame({
  x: 1,
  y: 1,
  width: 6,
  height: 4,
  data: new Uint8Array(fs.readFileSync("video.mp4")),
  type: "mp4",
  poster: new Uint8Array(fs.readFileSync("poster.jpg")),
  posterType: "jpg",
});
```

::

If no `poster` is provided, a minimal transparent placeholder is used automatically.

## Audio

```ts
import { AudioFrame } from "@office-open/pptx";
```

::api-example{type="pptx"}

```json [JSON]
{
  "slides": [
    {
      "children": [
        {
          "audio": {
            "x": 1,
            "y": 4,
            "width": 2,
            "height": 0.5,
            "data": "<Uint8Array>",
            "type": "mp3"
          }
        }
      ]
    }
  ]
}
```

```ts [Class]
{
  children: [
    new AudioFrame({
      x: 1,
      y: 4,
      width: 2,
      height: 0.5,
      data: new Uint8Array(fs.readFileSync("audio.mp3")),
      type: "mp3",
    }),
  ],
}
```

::

## Supported Formats

### Video (`type`)

| Value | Format |
| ----- | ------ |
| `mp4` | MP4    |
| `mov` | MOV    |
| `wmv` | WMV    |
| `avi` | AVI    |

### Audio (`type`)

| Value | Format |
| ----- | ------ |
| `mp3` | MP3    |
| `wav` | WAV    |
| `wma` | WMA    |
| `aac` | AAC    |

## Options

### VideoFrame

| Property     | Type                               | Default | Description            |
| ------------ | ---------------------------------- | ------- | ---------------------- |
| `x`          | `number`                           | `0`     | Left position (pixels) |
| `y`          | `number`                           | `0`     | Top position (pixels)  |
| `width`      | `number`                           | `0`     | Width (pixels)         |
| `height`     | `number`                           | `0`     | Height (pixels)        |
| `data`       | `Uint8Array`                       | -       | Video file data        |
| `type`       | `"mp4" \| "mov" \| "wmv" \| "avi"` | -       | Video format           |
| `name`       | `string`                           | auto    | Display name           |
| `poster`     | `Uint8Array`                       | auto    | Poster image data      |
| `posterType` | `"png" \| "jpg"`                   | `"png"` | Poster image format    |
| `animation`  | `IAnimationOptions`                | -       | Play animation         |

### AudioFrame

| Property    | Type                               | Default | Description            |
| ----------- | ---------------------------------- | ------- | ---------------------- |
| `x`         | `number`                           | `0`     | Left position (pixels) |
| `y`         | `number`                           | `0`     | Top position (pixels)  |
| `width`     | `number`                           | `0`     | Width (pixels)         |
| `height`    | `number`                           | `0`     | Height (pixels)        |
| `data`      | `Uint8Array`                       | -       | Audio file data        |
| `type`      | `"mp3" \| "wav" \| "wma" \| "aac"` | -       | Audio format           |
| `name`      | `string`                           | auto    | Display name           |
| `animation` | `IAnimationOptions`                | -       | Play animation         |

## Tips

- Provide a poster image for videos to ensure a visual preview is shown before playback.
- Audio frames are typically small and positioned at the bottom of a slide.
- Media files are embedded directly into the `.pptx` file — keep file sizes reasonable for performance.
- Supported playback depends on the presentation viewer (PowerPoint, Keynote, Google Slides, etc.).

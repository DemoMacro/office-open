import { XmlComponent } from "@file/xml-components";

/**
 * a:noFill — No fill (transparent).
 */
export class NoFill extends XmlComponent {
    public constructor() {
        super("a:noFill");
    }
}
